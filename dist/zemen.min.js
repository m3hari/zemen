(() => {
  var __defineProperty = Object.defineProperty;
  var __commonJS = (callback, module) => () => {
    if (!module) {
      module = {exports: {}};
      callback(module.exports, module);
    }
    return module.exports;
  };
  var __markAsModule = (target) => {
    return __defineProperty(target, "__esModule", {value: true});
  };
  var __export = (target, all) => {
    __markAsModule(target);
    for (var name in all)
      __defineProperty(target, name, {get: all[name], enumerable: true});
  };

  // src/index.ts
  var require_src = __commonJS((exports) => {
    __export(exports, {
      Zemen: () => Zemen,
      ethiopicToGregorian: () => ethiopicToGregorian,
      gregorianToEthiopic: () => gregorianToEthiopic
    });
    class Zemen {
      constructor(date) {
      }
      format(pattern) {
      }
      toString() {
      }
      getYear() {
      }
      getMonth() {
      }
      getDay() {
      }
      toGeregorian() {
      }
    }
  });

  // src/conversion/core.ts
  const GEREGORIAN_EPOCH = 1;
  const ETHIOPIC_EPOCH = 2796;
  const COPTIC_EPOCH = 103605;
  function rdFromCoptic({year, month, day}) {
    return COPTIC_EPOCH - 1 + 365 * (year - 1) + Math.floor(year / 4) + 30 * (month - 1) + day;
  }
  function copticFromRd(date) {
    const year = Math.floor(1 / 1461 * (4 * (date - COPTIC_EPOCH) + 1463));
    const month = Math.floor(1 / 30 * (date - rdFromCoptic({year, month: 1, day: 1}))) + 1;
    const day = date + 1 - rdFromCoptic({year, month, day: 1});
    return {year, month, day};
  }
  function rdFromEthiopic(SimpleDate3) {
    return ETHIOPIC_EPOCH + rdFromCoptic(SimpleDate3) - COPTIC_EPOCH;
  }
  function ethiopicFromRd(date) {
    return copticFromRd(date + COPTIC_EPOCH - ETHIOPIC_EPOCH);
  }
  function isGeregorianLeapYear(year) {
    return year % 4 === 0 && ![100, 200, 300].includes(year % 400);
  }
  function rdFromGeregorian({year, month, day}) {
    const step1 = GEREGORIAN_EPOCH - 1 + 365 * (year - 1) + Math.floor((year - 1) / 4) - Math.floor((year - 1) / 100);
    const step2 = Math.floor((year - 1) / 400) + Math.floor(1 / 12 * (367 * month - 362));
    const step3 = (month < 2 ? 0 : isGeregorianLeapYear(year) ? -1 : -2) + day;
    return step1 + step2 + step3;
  }
  function gregorianNewYear(year) {
    return rdFromGeregorian({year, month: 1, day: 1});
  }
  function geregorianYear(date) {
    const d0 = date - GEREGORIAN_EPOCH;
    const d1 = d0 % 146097;
    const d2 = d1 % 36524;
    const d3 = d2 % 1461;
    const n400 = Math.floor(d0 / 146097);
    const n100 = Math.floor(d1 / 36524);
    const n4 = Math.floor(d2 / 1461);
    const n1 = Math.floor(d3 / 365);
    const year = 400 * n400 + 100 * n100 + 4 * n4 + n1;
    return n100 === 4 || n1 === 4 ? year : year + 1;
  }
  function geregorianFromRd(date) {
    const year = geregorianYear(date);
    const priorDays = date - gregorianNewYear(year);
    const correction = date < rdFromGeregorian({year, month: 3, day: 1}) ? 0 : isGeregorianLeapYear(year) ? 1 : 2;
    const month = Math.floor(1 / 367 * (12 * (priorDays + correction) + 373));
    const day = date - rdFromGeregorian({year, month, day: 1}) + 1;
    return {year, month, day};
  }

  // src/conversion/index.ts
  function ethiopicToGregorian(date) {
    return geregorianFromRd(rdFromEthiopic(date));
  }
  function gregorianToEthiopic(date) {
    return ethiopicFromRd(rdFromGeregorian(date));
  }
  require_src();
})();
